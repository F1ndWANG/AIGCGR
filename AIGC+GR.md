# AIGC 发展现状与生成式推荐进展分析

> 时间基准：2026 年 4 月 27 日

## 1. 总体判断

截至 2026 年 4 月，AIGC 已经从早期的“模型能力竞赛”进入“系统化落地竞赛”。行业关注点正在从“模型能不能生成高质量内容”，转向“模型能否稳定、低成本、可控地嵌入真实业务流程，并持续产生可衡量价值”。

生成式推荐（Generative Recommendation, GR）则处在从研究探索走向架构重构的阶段。它已经不是单纯的概念验证，但距离全面替代传统推荐系统仍有明显距离。更现实的发展路径是：传统推荐系统继续承担高吞吐、低时延、强约束的主干能力，生成式模型逐步增强语义理解、对话交互、偏好控制、解释生成和生成式召回。

一个简明结论是：

- AIGC 已经进入企业常态化使用阶段，但真正产生利润表影响的组织仍然有限。
- 生成式推荐已经形成清晰研究主线，正在从“推荐排序”走向“推荐生成、偏好对齐和自然语言交互”。
- 未来主流形态更可能是“推荐系统生成化”，而不是“LLM 直接取代推荐系统”。

## 2. AIGC 当前发展阶段

### 2.1 从能力展示走向业务系统

AIGC 的发展可以粗略分为三个阶段：

1. 第一阶段：模型是否具备生成能力。
2. 第二阶段：生成质量是否达到可用标准。
3. 第三阶段：能否稳定接入企业工作流并产生商业价值。

目前行业已经进入第三阶段。Stanford HAI 的 2025 AI Index 显示，2024 年全球生成式 AI 私人投资达到 339 亿美元，同比增长 18.7%；同时，78% 的组织已在至少一个业务环节使用 AI，高于上一年的 55%。这说明 AIGC 已经从技术热点扩散为广泛采用的基础能力。

同一报告还指出，达到 GPT-3.5 水平的推理成本在 2022 年 11 月到 2024 年 10 月之间下降了 280 倍以上。开源或开放权重模型与闭源模型在部分基准上的差距，也从 8% 缩小到 1.7%。这意味着，AIGC 的竞争已经不再只是模型参数规模竞争，而是综合成本、部署能力、数据能力、产品集成和治理能力的竞争。

Stanford AI Index 2026 经济章节引用 McKinsey 2025 调研进一步指出，2025 年有 88% 的组织在至少一个业务职能中使用 AI，79% 的组织已在至少一个业务职能中经常使用生成式 AI。这表明 AIGC 已经进入企业常态化使用阶段。

### 2.2 采用率高，但价值兑现仍不充分

虽然企业采用率快速上升，但 AIGC 的商业价值兑现并不均衡。McKinsey 2025 年关于 AI 状态的报告指出，许多企业已经部署生成式 AI，但真正显著体现在盈利能力上的案例仍然有限。其后续关于 Agentic AI 的研究也强调，超过 80% 的企业尚未看到生成式 AI 对盈利产生实质贡献。

这说明 AIGC 当前的主要矛盾已经变化：

- 过去的主要问题是模型是否足够强。
- 现在的主要问题是组织是否能把模型转化为稳定流程、可靠产品和可度量收益。

企业真正获得收益，通常需要同时具备以下条件：

- 高质量内部数据。
- 明确的业务场景。
- 可嵌入现有系统的工程架构。
- 对模型输出的评估、审计和治理能力。
- 围绕 AI 重构后的组织流程。

## 3. AIGC 的主要发展趋势

### 3.1 多模态统一

AIGC 正在从文本生成扩展为多模态统一能力。文本、图像、语音、视频、代码、文档和结构化数据正在被整合到同一类模型或同一套应用系统中。

多模态能力带来的变化包括：

- 内容生产从单一文案扩展到图文、视频和交互内容。
- 搜索和推荐从关键词匹配扩展到语义、图像、视频和行为信号理解。
- 企业应用从聊天助手扩展到文档处理、会议理解、设计生成、代码生成和数据分析。

### 3.2 Agent 化

Agentic AI 是 2025 到 2026 年 AIGC 发展的核心方向之一。模型不再只是被动回答问题，而是开始具备规划、调用工具、执行多步任务和反馈修正的能力。

典型形态包括：

- 自动调用搜索、数据库、代码执行器、办公软件和业务系统。
- 分解复杂任务并逐步执行。
- 在执行过程中根据中间结果调整计划。
- 将自然语言目标转化为具体操作。

但 Agent 化也带来更高风险，例如错误动作、权限误用、隐私泄露、审计困难和不可预测行为。因此，真正可用的 Agent 系统通常需要权限边界、工具白名单、执行日志、人工确认点和结果校验。

### 3.3 成本工程化

随着 AIGC 进入真实业务场景，成本成为核心问题。企业不会只追求最大模型，而是会根据任务复杂度选择合适模型。

常见策略包括：

- 大模型处理复杂推理和高价值任务。
- 小模型处理高频、低复杂度任务。
- 使用模型路由在不同模型之间动态选择。
- 使用 RAG 降低对长上下文和模型记忆的依赖。
- 使用缓存、蒸馏、量化和本地部署降低推理成本。

未来成熟的 AIGC 系统往往不是单一模型，而是多模型、多工具、多数据源的组合系统。

### 3.4 工作流重构

AIGC 的最大价值不在单个聊天窗口，而在工作流重构。客服、销售、研发、运营、内容生产、教育、金融、医疗和法律等领域，都需要把 AI 嵌入实际流程。

例如：

- 客服场景中，AI 不只是回答问题，还要读取订单、判断责任、生成工单和辅助人工处理。
- 研发场景中，AI 不只是生成代码，还要理解代码库、运行测试、修复 CI、生成文档和参与代码审查。
- 内容场景中，AI 不只是写文案，还要结合用户画像、平台规则和转化目标生成多版本内容。

### 3.5 治理与合规

AIGC 的治理问题正在成为企业落地的硬约束。主要风险包括：

- 幻觉和事实错误。
- 数据隐私泄露。
- 版权和训练数据争议。
- 输出偏见和歧视。
- 高风险行业中的错误决策。
- 模型行为不可解释。

因此，企业级 AIGC 系统需要建立评估集、输出审计、人工复核、权限控制、日志追踪和合规策略。

## 4. 生成式推荐的概念边界

“AIGC + 推荐”通常包含两个层次。

第一类是 AIGC 赋能推荐外围能力，例如：

- 生成推荐理由。
- 生成商品标题、卖点、封面和短视频脚本。
- 做对话式导购。
- 用 LLM 理解用户评论、搜索词和自然语言需求。
- 对内容或商品进行语义标注。

这类应用已经相对成熟，因为它们通常不直接改变推荐系统主链路。

第二类才是真正意义上的生成式推荐：把推荐本身建模为生成问题。传统推荐系统通常采用“召回、粗排、精排、重排”的判别式范式，而生成式推荐尝试让模型直接生成下一个 item、item token、语义 ID、候选集合，或同时生成推荐结果、解释和对话回复。

生成式推荐的核心变化是：

- 从“对候选物品打分”转向“生成可能相关的物品”。
- 从“隐式学习用户兴趣”转向“结合自然语言显式偏好”。
- 从“静态推荐列表”转向“可交互、可解释、可调控的推荐过程”。

## 5. 生成式推荐的主要进展

### 5.1 从排序器到生成器

传统推荐系统将推荐任务拆解为召回和排序。模型主要判断用户对某个候选物品的点击、购买或停留概率。

生成式推荐则将推荐视为序列生成任务。例如，用户历史行为可以被看作一个序列，模型需要预测用户接下来可能感兴趣的物品。Meta 在 2024 年的论文《Actions Speak Louder than Words》中提出将推荐重述为 sequential transduction 问题，并设计了 HSTU 架构用于高基数、强时变和流式推荐数据。

这种范式的优势包括：

- 更自然地建模用户长期行为序列。
- 更容易吸收 Transformer 和语言模型中的序列建模能力。
- 有机会统一召回、序列预测和候选生成。

但挑战也很明显：

- item 空间巨大，直接生成物品 ID 难度高。
- 长尾物品难以充分学习。
- 在线推荐对延迟和吞吐要求极高。
- 生成结果需要满足库存、合规、商业规则和多样性约束。

### 5.2 行为信号与语义信号融合

推荐系统长期依赖用户行为数据，例如点击、购买、收藏、停留和跳出。但纯行为序列存在语义表达不足的问题。相反，LLM 擅长理解文本语义，但如果脱离真实交互反馈，推荐效果也会不稳定。

因此，生成式推荐的重要方向是融合行为信号和语义信号。2024 年的 EAGER 提出双流生成式推荐框架，将行为信息和语义信息协同建模。这类工作反映出一个行业共识：生成式推荐不是简单用 LLM 替代推荐系统，而是把推荐系统的行为建模能力与生成模型的语义理解能力结合起来。

融合后的系统可以更好地处理：

- 冷启动商品。
- 长尾内容。
- 用户自然语言需求。
- 跨域推荐。
- 推荐解释生成。

### 5.3 显式偏好控制

传统推荐系统主要从历史行为中隐式推断用户偏好。这种方式有效，但容易产生两个问题：

- 用户当下需求变化时，系统响应较慢。
- 系统可能持续强化用户过去的兴趣，形成信息茧房。

2024 年 12 月的论文《Preference Discerning with LLM-Enhanced Generative Retrieval》提出 preference discerning 范式，在上下文中显式加入用户自然语言偏好，使模型能够根据用户当前表达的需求动态调整推荐方向。

这代表生成式推荐的重要趋势：推荐系统从“根据过去行为猜测用户”转向“理解用户当前明确表达的意图”。

典型例子包括：

- 用户说“我想看节奏慢一点、不要太吵的科幻电影”，系统应立即改变推荐。
- 用户说“预算 300 元以内，适合通勤，不要太商务”，系统应根据显式约束生成候选商品。
- 用户说“最近不要再推荐类似内容”，系统应快速更新短期偏好。

这种能力对于电商、内容流、视频平台、音乐平台、旅游和本地生活推荐都很重要。

### 5.4 推荐、解释和对话一体化

生成式推荐的另一条重要路线是把推荐结果、推荐解释和对话交互统一起来。

Google Research 在 2025 年公开的 REGEN 项目中，强调推荐系统不仅要给出 item，还要支持自然语言批评、反馈和叙述性解释。其思路包括：

- 混合架构：传统序列推荐器生成 item，LLM 生成解释和自然语言回复。
- 统一架构：一个模型同时决定何时输出 item token、何时输出自然语言。

这类方向对应真实产品中的对话式搜索、智能导购和个性化内容助手。用户不再只是被动接收推荐列表，而是可以通过自然语言持续修正推荐结果。

## 6. 工业落地现状

生成式推荐在工业界已经受到重视，但并未大规模全面替代传统推荐系统。主要原因是推荐系统本身具有极高工程复杂度。

### 6.1 传统推荐系统仍然具有强优势

传统推荐系统经过多年优化，形成了成熟的多阶段架构：

- 召回层负责从海量物品中快速筛出候选。
- 粗排层负责快速过滤低质量候选。
- 精排层负责高精度预测点击率、转化率或停留时长。
- 重排层负责多样性、去重、业务规则和生态约束。

这一架构的优势是吞吐高、延迟低、可控性强、工程可解释性好。生成式模型如果直接替代整条链路，会面临成本、延迟、稳定性和约束处理问题。

### 6.2 生成式推荐更可能先从局部模块切入

更现实的落地路径包括：

- 使用 LLM 理解用户自然语言需求。
- 使用生成式模型生成候选集合，再交给传统排序模型精排。
- 使用 LLM 生成推荐解释和导购话术。
- 使用生成式检索处理长尾内容和冷启动物品。
- 使用对话式交互收集用户显式偏好。
- 使用多模态模型理解商品图像、视频封面、评论和内容语义。

因此，近期工业系统更可能采用混合架构，而不是纯生成式架构。

## 7. 关键挑战

### 7.1 巨大 item 空间

推荐系统面对的物品空间往往非常大。电商平台可能有数亿商品，内容平台可能有海量视频、图文和直播间。让生成模型直接生成正确 item，难度远高于生成自然语言 token。

常见解决方向包括：

- 将 item 映射为语义 ID。
- 使用分层生成。
- 使用向量索引结合生成式检索。
- 先生成候选类别或属性，再召回具体物品。

### 7.2 实时性和成本

推荐系统通常要求毫秒级响应。大型生成模型推理成本高、延迟高，很难直接承载所有在线请求。

可行策略包括：

- 离线生成候选或解释。
- 在线使用小模型。
- 使用缓存复用结果。
- 只在高价值场景调用大模型。
- 将生成式模块放在召回或交互层，而不是每次精排都调用。

### 7.3 约束与可控性

真实推荐需要满足多种约束：

- 库存。
- 价格。
- 广告预算。
- 合规规则。
- 内容安全。
- 多样性。
- 新内容扶持。
- 用户负反馈。
- 平台生态健康。

生成模型天然擅长自由生成，但不天然擅长严格满足硬约束。因此生成式推荐必须与规则系统、检索系统、排序系统和安全系统结合。

### 7.4 评估困难

推荐系统最终看线上指标，例如：

- CTR。
- CVR。
- GMV。
- 停留时长。
- 复购率。
- 负反馈率。
- 留存率。
- 多样性。
- 长期用户满意度。

生成式推荐在公开数据集上取得提升，并不意味着线上一定有效。工业落地必须经过 A/B 测试、长期指标观察和风险控制。

## 8. 未来 1 到 3 年趋势

未来 AIGC 和生成式推荐可能沿以下方向发展：

1. AIGC 将从通用聊天继续走向行业工作流 AI。
2. 企业会更多采用多模型组合，而不是单一大模型。
3. Agent 能力会增强，但会和权限、审计、工具调用边界一起发展。
4. 推荐系统会逐步语言化、对话化和生成化。
5. 生成式推荐会优先在自然语言导购、对话式搜索、推荐解释、冷启动和生成式召回中落地。
6. 传统召回排序架构不会快速消失，而是和生成式模块长期共存。
7. 多模态推荐会变得更重要，尤其在电商、短视频、音乐、影视和本地生活场景。
8. 显式偏好控制会成为下一代推荐体验的重要能力。

## 9. 对项目和研究的启示

如果以“AIGC + 生成式推荐”为项目或研究方向，可以优先考虑以下切入点：

- 对话式推荐：让用户通过自然语言表达偏好，系统动态调整推荐。
- 生成式召回：使用生成模型生成候选 item 或语义 ID，再接传统排序。
- 推荐解释生成：为推荐结果生成可信、简洁、个性化的解释。
- 多模态推荐：融合文本、图像、视频、行为序列和用户反馈。
- 冷启动推荐：利用商品描述、图像、评论和类目信息生成初始表示。
- 偏好纠偏：允许用户显式表达“不想看什么”，降低信息茧房。
- 混合架构：把 LLM 用在语义理解和交互层，把传统模型保留在高频排序链路。

从工程角度看，一个可行的项目架构可以是：

1. 用户输入自然语言需求。
2. LLM 抽取偏好、约束和场景。
3. 向量检索或生成式召回产生候选集合。
4. 传统排序模型进行精排。
5. 规则层处理合规、库存、价格、多样性和去重。
6. LLM 生成推荐解释或对话回复。
7. 用户反馈再次进入短期偏好状态。

这种混合方案比纯 LLM 推荐更容易落地，也更符合当前工业趋势。

## 10. 结论

AIGC 的发展已经进入深水区。模型能力仍然重要，但真正决定价值的是工程化、数据化、流程化和治理能力。企业不再只是比较模型参数，而是在比较谁能把模型嵌入业务系统并形成稳定收益。

生成式推荐是推荐系统未来的重要方向，但它不会简单替代传统推荐系统。更准确的判断是，推荐系统会逐步吸收生成式模型的能力，变得更懂语义、更可对话、更可解释、更能响应显式偏好。

未来 1 到 3 年，最值得关注的不是“LLM 是否彻底取代推荐系统”，而是“生成式能力如何重构推荐链路中的召回、交互、解释和偏好控制”。在这个过程中，混合架构会是最现实、最有产业价值的路径。

## 参考资料

- Stanford HAI, AI Index 2025: https://hai.stanford.edu/ai-index/2025-ai-index-report%E2%80%8B
- Stanford HAI, AI Index 2026 Economy Chapter: https://hai.stanford.edu/assets/files/ai_index_report_2026_chapter_4_economy.pdf
- McKinsey, The State of AI 2025: https://www.mckinsey.com/capabilities/quantumblack/our-insights/the-state-of-ai-how-organizations-are-rewiring-to-capture-value
- McKinsey, Seizing the Agentic AI Advantage: https://www.mckinsey.com/~/media/mckinsey/business%20functions/quantumblack/our%20insights/seizing%20the%20agentic%20ai%20advantage/seizing-the-agentic-ai-advantage.pdf
- Meta, Actions Speak Louder than Words: https://arxiv.org/abs/2402.17152
- EAGER: https://arxiv.org/abs/2406.14017
- Preference Discerning with LLM-Enhanced Generative Retrieval: https://arxiv.org/abs/2412.08604
- Google Research, REGEN: https://research.google/blog/regen-empowering-personalized-recommendations-with-natural-language/
- Gen-RecSys Survey: https://arxiv.org/abs/2404.00579
